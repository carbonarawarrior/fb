 # fb

 flappy bird esque game written with [raylib](https://github.com/raysan5/raylib)
 build using:
 `make MODE=release`

failure to do so results in debug build.
binary can be found under
`build/[mode]/fb`
