#ifndef MAIN_HEADER
#define MAIN_HEADER

#include <stddef.h>
static const size_t screen_width =  350 * 4;
static const size_t screen_height = 350 * 3;
static const int target_fps = 60;

static const float acc_gravity = 30.0;
static const float ground_level = 100.0;

#endif
